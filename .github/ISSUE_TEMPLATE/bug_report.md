---
name: Bug report
about: Something behaves differently from what the documentation says
labels: bug
---

Concrete details beat polished prose here; include the smallest example that reproduces the issue.

**Command run**
```bash
# the exact command, please
```

**Observed output**
```
# paste it, including the stop reason if a mission ended
```

**Expected** — and where the documentation says so (file + line helps).

**Environment**
- Python version (`python -VV`):
- OS:
- ATHERYN version (`python -c "import atheryn; print(atheryn.__version__)"`):

**Mission state, if relevant**
```
python -m atheryn verify --dir <mission>
python -m atheryn status --dir <mission>
```

Please don't paste API keys or private document content.
